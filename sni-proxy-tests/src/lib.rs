//! Empty lib for the sni-proxy-tests crate.
//!
//! All tests live under `tests/<module>.rs`; each one is its own
//! Cargo integration-test binary. The crate exists so cargo's
//! workspace tracking has a place to hang those test files —
//! there's no production code here.
